<!--<?xml version="1.0" encoding="UTF-8"?>-->

<!--<!DOCTYPE hibernate-configuration PUBLIC-->
<!--        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"-->
<!--        "https://hibernate.org/dtd/hibernate-configuration-3.0.dtd">-->

<hibernate-configuration>

    <session-factory>

        <!-- ============================= -->
        <!-- Database Connection Settings -->
        <!-- ============================= -->

        <property name="hibernate.connection.driver_class">
            com.mysql.cj.jdbc.Driver
        </property>

        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/hibernate_db
        </property>

        <property name="hibernate.connection.username">
            root
        </property>

        <property name="hibernate.connection.password">
            123@rohit@123
        </property>

        <!-- ============================= -->
        <!-- Hibernate Settings -->
        <!-- ============================= -->

        <!-- SQL dialect for MySQL -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.MySQLDialect
        </property>

        <!-- Automatically update database schema -->
        <property name="hibernate.hbm2ddl.auto">
            update
        </property>

        <!-- Print SQL in the console -->
        <property name="hibernate.show_sql">
            true
        </property>

        <!-- Format SQL nicely -->
        <property name="hibernate.format_sql">
            true
        </property>

        <!-- Highlight SQL in supported consoles -->
        <property name="hibernate.highlight_sql">
            true
        </property>

        <!-- ============================= -->
        <!-- Entity Mappings -->
        <!-- ============================= -->

        <!--
        We will uncomment this after creating Student.java

        <mapping class="com.rohit.entity.Student"/>
        -->

    </session-factory>

</hibernate-configuration>